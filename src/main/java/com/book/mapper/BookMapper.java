package com.book.mapper;

import com.book.dto.BookDTO;
import com.book.model.Book;

import java.util.Optional;

public interface BookMapper {
/*default Book mapRequestToModel(BookDTO bookDTO,Book book){
   / Optional.ofNullable(bookDTO.getBody()).ifPresent(book.setBody(bookDTO.getBody()))   ;

}*/
}
